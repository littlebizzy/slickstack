#!/bin/bash

####################################################################################################
#### author: SlickStack ############################################################################
#### link: https://slickstack.io ###################################################################
#### mirror: http://mirrors.slickstack.io/5-cron-half-daily.txt ####################################
#### path: /var/www/5-cron-half-daily ##############################################################
#### destination: n/a (not a boilerplate) ##########################################################
#### purpose: SlickStack cron jobs "half daily" (5/9) ##############################################
#### ss version: SlickStack alpha ss6a #############################################################
#### module version: n/a ###########################################################################
####################################################################################################

## !!! DO NOT MODIFY THIS FILE. ADD TEMPORARY BASH SCRIPTS TO SS-WORKER INSTEAD. ##

## scripts to run (include ss-clean here) ##
source /var/www/ss-clean

# if [[ "$SS_DUMP" == "true" && "$SS_INTERVAL_DUMP" == "half-daily" ]]; then 
#    source /var/www/ss-dump
# fi

# if [[ "$SS_PERMS" == "true" && "$SS_INTERVAL_PERMS" == "half-daily" ]]; then 
#    source /var/www/ss-perms
# fi

# if [[ "$SS_SCAN" == "true" && "$SS_INTERVAL_SCAN" == "half-daily" ]]; then 
#    source /var/www/ss-scan
# fi

## delete lock files ##
rm -R -f /tmp/5-cron-half-daily*.lock*
