/* LITTLEBIZZY CONFIG -- DO NOT DELETE BELOW LINES */

/* ensure WordPress has enough memory */
define('WP_MEMORY_LIMIT', '512M');
define('WP_MAX_MEMORY_LIMIT', '512M');

/* optimize file creation and file permission settings */
define('FS_METHOD', 'direct');
define('FS_CHMOD_DIR', (0755 & ~ umask()));
define('FS_CHMOD_FILE', (0644 & ~ umask()));

/* debugging and error logging */
define('WP_DEBUG', false); /* enable to send debug info to /var/www/logs/error.log */
define('WP_DEBUG_LOG', false); /* reduntant (unnecessary) on SlickStack */
define('WP_DEBUG_DISPLAY', false); /* should never be enabled on production (live) websites */

/* php.ini manipulation not needed on SlickStack */
// ini_set('log_errors', 'On'); /* already enabled on SlickStack */
// ini_set('error_log', '/var/www/logs/error.log'); /* error log for SlickStack = /var/www/logs/error.log */

/* limit the number of post revisions */
define('WP_POST_REVISIONS', 10);

/* limit WP-Cron calls to avoid server overload */
define('WP_CRON_LOCK_TIMEOUT', 300);

/* extend trash life to avoid data loss (a backup function in case Disable Empty Trash plugin not installed) */
define('EMPTY_TRASH_DAYS', 9999999999);

/* disable annoying nag notices in WP Admin */
define('DISABLE_NAG_NOTICES', true);

/* wordpress temp directory */
define('WP_TEMP_DIR','/var/www/html/wp-content/temp');

/* contact form 7 tmp directory */
define( 'WPCF7_UPLOADS_TMP_DIR', '/var/www/html/wp-content/temp' );

/* LITTLEBIZZY CONFIG -- DO NOT DELETE ABOVE LINES */
