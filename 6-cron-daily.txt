#!/bin/bash

####################################################################################################
#### author: SlickStack ############################################################################
#### link: https://slickstack.io ###################################################################
#### mirror: http://mirrors.slickstack.io/6-cron-daily.txt #########################################
#### path: /var/www/6-cron-daily ###################################################################
#### destination: n/a (not a boilerplate) ##########################################################
#### purpose: SlickStack cron jobs "daily" (6/9) ###################################################
#### ss version: SlickStack alpha ss6a #############################################################
#### module version: n/a ###########################################################################
####################################################################################################

## !!! DO NOT MODIFY THIS FILE. ADD TEMPORARY BASH SCRIPTS TO SS-WORKER INSTEAD. ##

## scripts to run (include ss-muplugs + ss-scan here) ##
source /var/www/ss-muplugs
source /var/www/ss-scan

# if [[ "$SS_INTERVAL_CHECK" == "daily" ]]; then 
#    source /var/www/ss-check
# fi

## run ss-worker if interval set to "regular" or if not defined ##
# if [[ "$SS_INTERVAL_WORKER" == "daily" ]]; then 
#    source /var/www/ss-worker
# fi

# if [[ "$SS_INTERVAL_DUMP" == "daily" ]]; then 
#    source /var/www/ss-dump
# fi

# if [[ "$SS_INTERVAL_PERMS" == "daily" ]]; then 
#    source /var/www/ss-perms
# fi

# if [[ "$SS_INTERVAL_CLEAN" == "daily" ]]; then 
#    source /var/www/ss-clean
# fi

# if [[ "$SS_INTERVAL_MUPLUGS" == "daily" || -z "$SS_INTERVAL_MUPLUGS" ]]; then 
#    source /var/www/ss-muplugs
# fi

# if [[ "$SS_INTERVAL_SCAN" == "daily" || -z "$SS_INTERVAL_SCAN" ]]; then 
#    source /var/www/ss-scan
# fi

## delete lock files ##
rm -R -f /tmp/6-cron-daily*.lock*
